*Last updated: July 30, 2026 — Session 190 — **Subscription cancel-choice (immediate vs. end-of-term) + scheduled start date (up to 30 days out), prompted by the Olivia Rosaldo-Pratt incident.** Olivia opted into Subscription on Jul 22 wanting an Aug 1 start; staff refunded her $275 on Jul 28 believing the subscription was cancelled immediately, but `cancel-subscription` had exactly one behavior for a paid-up subscription — always `cancel_at_period_end=true` — so she stayed listed "active" through her Aug 22 term end. Two features shipped to close this gap for good. **Feature 1 (`cancel-subscription` v11):** on an otherwise-healthy (active/paused) subscription, admin now gets an explicit choice — "Cancel immediately" or "Cancel at end of term" — instead of always silently taking the period-end path. New `immediate` request param, gated to staff (`assertOwnership` extended to recognize `manager` as staff, returns `{ok, isStaff}`); past_due and trialing subscriptions remain forced-immediate regardless of the flag (no unpaid invoice or not-yet-started subscription should ever "run out a term"). Response now carries a `reason` (`past_due` / `trialing_scheduled` / `staff_immediate`) for logging clarity. **Feature 2 — scheduled subscription start date, customer + admin, ≤30 days out:** implemented with **zero DB migration** by reusing Stripe's native `trial_end`/`trialing` mechanism — `'trialing'` was already an allowed value in the `subscriptions.status` CHECK constraint, and `current_period_end` while trialing doubles as the scheduled-start date, so no new column was needed. `resolveTrialEndEpoch(startDate)` (Pacific-anchored, BIZ_TZ) validates the date is today..+30 and converts to a Stripe trial_end epoch; duplicated identically in `create-subscription` (v7 — customer-app saved-card + new-card paths, admin opt-in-with-card path) and `create-checkout` (v36 — admin "Start Checkout" fallback for a customer with no saved card). **`stripe-webhook` v50:** the pricelist-flip-to-'Subscription' on `customer.subscription.created` is now gated behind `subStatus === 'active'` (skipped while trialing, so a scheduled subscriber keeps normal pay-per-order pricing/booking until their chosen date); since Stripe doesn't fire a distinct "trial ended" event, a new block in `customer.subscription.updated` does the flip the moment status flows trialing→active. **UI (`admin-dashboard/index.html`, `customer-app/index.html`):** `'trialing'` recognized everywhere subscription status is read — badges ("SCHEDULED" / "Starts on {date}"), Reports subscriber table + status-color map, renewal-label logic; admin Customer panel gained a date input (`#cpb-start-date`, min=today/max=+30) threaded through both the opt-in and Start Checkout paths with scheduled-vs-immediate confirm-modal copy; customer-app subscribe modals (saved-card and new-card) gained the same date picker, with a dedicated "scheduled" home-card state and cancel-copy branch. **QA (washroute-qa) — clean, no issues found:** confirmed RLS on `subscriptions` has no status restriction (customer can read their own trialing row fine); confirmed `audit_subscription_pricelist_orphans`, `audit_subscriptions_missing_invoice`, `enforce_sub_not_past_due`, and `link_subscription_on_order_fn` all filter on `status IN ('active','past_due','paused')` and so correctly exclude `'trialing'` — a scheduled subscriber won't false-positive the daily audit, won't get subscription $0-delivery pricing early, and won't get their normal pay-per-order pickups auto-linked to the not-yet-started subscription; confirmed every `create-subscription`/`create-checkout` call site threads `startDate` correctly; confirmed customer-app only calls `showSubscriptionPickupPicker()` for an immediate-start subscription (a scheduled one correctly skips it and keeps normal booking, per David's confirmed requirement). All 4 edge functions deployed and live (`cancel-subscription` v11, `create-subscription` v7, `create-checkout` v36, `stripe-webhook` v50). ⚠️ **DEPLOY PENDING — UI not yet live:** `admin-dashboard/index.html` + `customer-app/index.html` are updated and written to disk in the project folder, but hit the sandbox's chronic `.git/index.lock` issue (see many prior session notes — a stale/regenerating lock the sandbox cannot remove) before the commit could go through here. **David needs to commit + push from his Mac terminal** — exact paste-block is in this session's chat. The 4 edge functions are backward-compatible, so nothing is broken live in the meantime; the two new UI affordances (cancel-choice buttons, start-date pickers) simply aren't visible yet. ⚠️ **Olivia's own subscription record is still out of sync** — Stripe + the `subscriptions` row both still show `status='active'`, `cancel_at_period_end=true`, `current_period_end='2026-08-22'` (her `customers.pricelist` was already separately reverted to Delivery/paygo by staff, so she isn't being mis-priced, but the subscription record itself is wrong). **Fix once this deploy is live:** open her Customer panel → Billing and click the new "Cancel immediately" button — this is the correct root-cause-consistent fix (syncs Stripe + DB together in one transaction, proper audit trail) rather than a raw DB patch, and is literally the mechanism Feature 1 was built to provide.*

