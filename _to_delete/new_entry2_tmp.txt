*Last updated: July 30, 2026 — Session 190 pt 2 — **Fixed a gap in the session-190 cancel-choice feature: an already-scheduled-to-cancel subscription had no "Cancel now" button at all.** Found immediately after deploy, on the exact account that motivated the feature: Olivia Rosaldo-Pratt's subscription is `status='active'` + `cancel_at_period_end=true` (staff placed a graceful cancel on Jul 28), which routes `renderCpSubscriptionActions` into the pre-existing `scheduling` branch (session 178) — and that branch only showed "Cancel now" when `status==='past_due'`, otherwise just "Reactivate". So the one scenario Feature 1 was built to fix (force an already-scheduled cancellation to end immediately) had no button for it. **Fix (`admin-dashboard/index.html`, `renderCpSubscriptionActions`):** the `scheduling` branch now always pairs "↺ Reactivate" with "✕ Cancel now" (`adminCancelSubscription(id, true)`), regardless of past_due — no functional risk since `cancel-subscription` v11 already forces immediate for past_due and this just extends the same button to the non-past-due scheduled case. **Immediate workaround used to unblock Olivia same-day** (before this fix deployed): Reactivate → re-render flips her into the "healthy" branch (both buttons) → Cancel immediately. **Lesson for QA:** when a new status-gated UI branch is added, audit for *every* pre-existing branch that could also match the same record — a session-178 branch shipped fine on its own but wasn't revisited when session-190 added a sibling branch below it in the same if/else chain, and the combination left one real production account with no working button. Committed + pushed to `main` via the `laptop-untracked-2026-07-24` local branch (`git push origin laptop-untracked-2026-07-24:main` — this Mac's checkout has been sitting on that branch, not `main`, since a prior session's git-lock recovery; that refspec push is the safe way to land commits on `main` from here without switching branches).*

