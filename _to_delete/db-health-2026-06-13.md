# WashRoute DB Health Check — 2026-06-13 15:23 UTC

## 🔴 RED — real degradation happening now (not a full lockout yet)

The database still answers simple requests, but actual app queries (route stops, SMS) are **actively timing out** right now — roughly **24 statement-timeout errors in the last hour** of logs. This is the same pattern that preceded the May 26 lockout. It is not a full outage yet, so there's time to act.

**Do this now:** In the Supabase dashboard, go to **Settings → Compute and Disk** and **restart or bump the compute add-on**. That gives immediate relief.

**The real fix (schedule soon):** the index + RLS optimization work on the slow queries below. Bumping compute only buys time.

---

## The numbers

**1. Responsiveness** — ✅ `SELECT now()` returned instantly. Simple queries are fine; the DB is not locked up.

**2. Connections & activity**
- Total connections: 36 (moderate, not alarming)
- Active queries: 3
- Idle in transaction: 0 (good)
- Long-running >30s: 1 — but this is a **realtime replication worker (walsender)**, which is normal and expected. Not a stuck query.

**3. Recent errors (last ~1h of logs)** — 🔴 ~24 × "canceling statement due to statement timeout". Real requests are being killed. This is the leading indicator of IO strain.

**4. Slow queries (avg time)** — 🔴 several well over the 500ms line:
- `sms_messages` lookup — **3.7s avg**, 3,166 calls (biggest total load by far)
- `route_stops + orders` join — **5.8s avg**, 279 calls
- an RPC call (`p_fro...`) — **4.2s avg**, 52 calls
- (a `pg_sleep` test query at 15.9s is a manual/diagnostic call — ignore)

**5. Realtime load** — ✅ `realtime.subscription` = 51 rows. Small, not a concern.

---

## Plain-English summary

The system isn't down, but it's straining — the SMS and route-stop queries are slow enough that real requests time out about two dozen times an hour. Restart/bump compute today for breathing room, then book the index optimization work so this doesn't creep back toward another lockout.
